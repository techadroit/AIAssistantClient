The Application is a chat based application. Where the user can use the application to interact with llm.
The application supports multiple llm providers like openai, azure openai, cohere, anthropic etc.
The application uses compose multiplatform for building the ui. The application currently supports desktop platform. In future it will support android, ios and web platform.
